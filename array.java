/**
 * array
 */
public class array {

    public static void main(String[] args) {
        typ[] array = new typ[1]; // typ array []= new typ[1];
        person[] personlist = { jeni, kate, magi }; // person[] personlist = new person[] { jeni, susanne, magi };
        System.out.println(personlist);
        for (int i = 0; i < personlist.length; i++)
            ;
    }
}
